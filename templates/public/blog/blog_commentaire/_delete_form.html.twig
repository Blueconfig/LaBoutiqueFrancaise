<form method="post" action="{{ path('app_public_blog_blog_commentaire_delete', {'id': blog_commentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_commentaire.id) }}">
    <button class="btn">Delete</button>
</form>
